numbers = [1, 2, 3, 4, 5]
# What do the following method calls do (assume we reset numbers to the original array between method calls)?

# numbers.delete_at(1)
# p numbers
numbers.delete(1)
p numbers

# numbers = numbers.delete_at(1)

# numbers = numbers.delete(1)
# p numbers
